class AppConstants {
  static const String appName = 'Fast Food App';

  // Storage Keys
  static const String userTokenKey = 'user_token';
  static const String userIdKey = 'user_id';

  // Timeout
  static const Duration timeoutDuration = Duration(seconds: 30);
}
